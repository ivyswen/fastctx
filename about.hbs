# Third-party Rust dependency licenses

<!-- Generated by `cargo about generate`. Do not edit by hand: the CI supply-chain
     job regenerates this file and fails when the committed copy is stale. -->

Every crate below is statically linked into the published `fastctx` binaries, so
each applicable license text is reproduced here in full.

The bundled Pdfium build and its own third-party notices are covered separately
in [`THIRD_PARTY_LICENSES.md`](./THIRD_PARTY_LICENSES.md).

## Overview

{{#each overview}}
- {{{name}}}: {{count}}
{{/each}}

{{#each licenses}}
## {{{name}}}

Used by:

{{#each used_by}}
- [{{{crate.name}}} {{{crate.version}}}]({{#if crate.repository}}{{{crate.repository}}}{{else}}https://crates.io/crates/{{{crate.name}}}{{/if}})
{{/each}}

```text
{{{text}}}
```

{{/each}}
